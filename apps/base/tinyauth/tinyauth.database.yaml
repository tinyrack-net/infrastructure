apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: tinyauth-database-backup-store
  namespace: tinyauth-system
spec:
  retentionPolicy: 30d
  configuration:
    destinationPath: s3://tinyrack-prod/apps/tinyauth/database
    endpointURL: https://fsn1.your-objectstorage.com
    s3Credentials:
      accessKeyId:
        name: tinyrack-s3-secret
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: tinyrack-s3-secret
        key: AWS_SECRET_ACCESS_KEY
    data:
      compression: gzip
    wal:
      compression: gzip
      maxParallel: 8
---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: tinyauth-database-cluster
  namespace: tinyauth-system
spec:
  instances: 1
  imageName: ghcr.io/cloudnative-pg/postgresql:17-bookworm
  postgresql:
    parameters:
      max_connections: "500"
  storage:
    pvcTemplate:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 32Gi
      storageClassName: longhorn-strict-local
      volumeMode: Filesystem
  monitoring:
    enablePodMonitor: false

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: tinyauth-database-backup-store

  enableSuperuserAccess: true
  superuserSecret:
    name: tinyauth-database-superuser-secret

  bootstrap:
    initdb:
      database: tinyauth
      owner: tinyauth
      secret:
        name: tinyauth-database-user-secret
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: tinyauth-backup
  namespace: tinyauth-system
spec:
  schedule: "0 0 */4 * * *"
  backupOwnerReference: self
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  cluster:
    name: tinyauth-database-cluster
