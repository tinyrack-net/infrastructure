apiVersion: v1
kind: Service
metadata:
  name: litellm-service
  namespace: litellm-system
spec:
  selector:
    app: litellm
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 4000
  type: ClusterIP
